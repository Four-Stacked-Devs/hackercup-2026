{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm run contracts && npm run build",
  "build": {
    "env": {
      "NEXT_PUBLIC_API_MODE": "live"
    }
  },
  "env": {
    "NEXT_PUBLIC_API_MODE": "live"
  }
}
